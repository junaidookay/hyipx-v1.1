{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "touch core/database/database.sqlite && cd core && composer install --no-dev --optimize-autoloader --no-scripts && php artisan package:discover --ansi"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT index.php",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
